---
title: "Quickstart"
description: "Get started with the Snipp API in three steps"
---

## Get started in three steps

Make your first API call and upload a file.

### Step 1: Get your API key

1. Sign in at [snipp.gg](https://snipp.gg).
2. Open **Settings** and find your API key.
3. Copy the key and keep it secure. You use it in the `api-key` header for every request.

<Warning>
Do not share your API key or commit it to version control. Use environment variables in production.
</Warning>

### Step 2: Verify your setup

Test your API key by fetching your user data:

```bash
curl -X GET "https://api.snipp.gg/users/@me" \
  -H "api-key: YOUR_API_KEY"
```

A successful response returns your user object with profile fields, badge flags, a `badges` object, and `uploads`.

### Step 3: Upload a file

Upload an image or video:

```bash
curl -X POST "https://api.snipp.gg/upload" \
  -H "api-key: YOUR_API_KEY" \
  -F "file=@/path/to/your/image.png"
```

The response includes a `url` field with the direct link to your uploaded file.

## Next steps

<CardGroup cols={2}>
  <Card title="Authentication" icon="key" href="/authentication">
    Learn how the api-key header works.
  </Card>
  <Card title="API reference" icon="cloud-arrow-up" href="/api-reference/introduction">
    Explore upload options like privacy settings.
  </Card>
  <Card title="File types" icon="file" href="/concepts/file-types">
    See supported formats and size limits.
  </Card>
  <Card title="Rate limits" icon="gauge" href="/concepts/rate-limits">
    Understand request limits.
  </Card>
</CardGroup>
